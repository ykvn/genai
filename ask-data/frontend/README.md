# Frontend UI

Run the Gradio app with:

```bash
cd ask-data/frontend
python3 frontend_entry.py
```

Expected environment variables:
- `BACKEND_URL` (default: `http://127.0.0.1:8090/ask`)
- `CDSW_APP_PORT` (default: `8100`)
